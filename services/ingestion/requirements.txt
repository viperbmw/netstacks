# SNMP trap receiver
pysnmp==6.2.6
pysnmp-mibs==0.1.6

# HTTP clients
httpx==0.27.0
aiohttp==3.9.1

# Configuration
pydantic==2.5.3
pydantic-settings==2.1.0
pyyaml==6.0.1

# Kafka consumer (optional - uncomment if using Kafka)
aiokafka==0.10.0

# Redis streams consumer (optional - uncomment if using Redis)
redis==5.0.1
